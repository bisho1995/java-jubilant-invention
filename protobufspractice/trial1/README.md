You need to run the following command to pick up proto files from resources folder and generate Java files out of them.

```mvn clean compile install```

You can also set this as part of module configuration.
